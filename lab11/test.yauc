// Test file for YAUC compiler
//

rank = -5;

src_value = 5;
rank = 5;

if ( rank > 0 ) then
	while ( rank > 0 ) do
		src_value = src_value * src_value;
		rank = rank - 1;
	end;
else
	if ( rank != 0 ) then
		while ( rank < 0 ) do
			src_value = src_value * src_value;
			rank = rank + 1;
		end;
		src_value = src_value / rank;
	else
		src_value = 1;
	end;
end;

src_value = src_value + sin(5);

print(5,6,7);