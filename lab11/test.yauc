// Test file for YAUC compiler
//

src_value = 5;

cur_value = src_value;
rank = 5;

if ( rank > 0 ) then
	while ( rank > 0 ) do
		cur_value = cur_value * src_value;
		rank = rank - 1;
	end;
else
	if ( rank != 0 ) then
		while ( rank < 0 ) do
			src_value = src_value * src_value;
			rank = rank + 1;
		end;
		src_value = src_value / rank;
	else
		src_value = 1;
	end;
end;

print("test");
print(5 + cur_value);
